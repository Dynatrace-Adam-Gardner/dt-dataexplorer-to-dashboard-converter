---
spec_version: '1.0'
indicators:
  #sli1: 'builtin:host.cpu.usage:splitBy("dt.entity.host"):avg:auto:sort(value(avg,descending)):limit(10)'
  #sli2: 'builtin:host.cpu.usage:splitBy("dt.entity.host"):avg:auto:sort(value(avg,descending)):limit(20)'
  #sli3: builtin:host.cpu.usage:splitBy("dt.entity.host"):avg:auto:sort(value(avg,descending)):limit(30)
  #sli4: "builtin:host.cpu.usage:splitBy(\"dt.entity.host\"):avg:auto:sort(value(avg,descending)):limit(40)"
  #sli5: 'trivy.vulnerabilities.CRITICAL:splitBy(image,tag):avg:auto:sort(value(avg,descending)):limit(50)'
  #sli6: 'trivy.vulnerabilities.CRITICAL:splitBy(image,tag):avg:auto:sort(value(avg,descending))'
  #sli7: 'trivy.vulnerabilities.CRITICAL:splitBy(image):avg:auto:sort(value(avg,descending)):limit(10)'
  sli8: 'trivy.vulnerabilities.CRITICAL:filter(and(eq(tag,"1.21.0"))):splitBy(image):avg:auto:sort(value(avg,descending)):limit(10)'
  #sli9: 'trivy.vulnerabilities.CRITICAL:filter(and(eq(image,nginx))):splitBy(image):avg:auto:sort(value(avg,descending)):limit(10)'